# Frontend — Backend Explorer

This is a minimal Vite + React app that provides a simple UI to explore the local Express backend.

Quick start:

```bash
cd frontend
npm install
npm run dev
```

By default the client will call `http://localhost:3000`. To change the backend URL set `VITE_BACKEND_BASE` in an `.env` file.
